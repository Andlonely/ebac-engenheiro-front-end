function calculaArea(base, altura){
    area = base*altura
    return area;
}

let base = prompt("Digite a base: ");
let altura = prompt("Digite a altura: ");

alert("A Area é: "+calculaArea(base,altura));
